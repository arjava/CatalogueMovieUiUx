<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.cataloguiux" parent="Theme.AppCompat.Light">
        <item name="actionBarItemBackground">@color/actionBarItemBackground</item>
        <item name="popupMenuStyle">@color/popupMenuStyle</item>
        <item name="dropDownListViewStyle">@color/dropDownListviewStyle</item>
        <item name="actionBarTabStyle">@color/actionBarTabStyle</item>
        <item name="actionDropDownStyle">@color/actionDropDownStyle</item>
        <item name="actionBarStyle">@color/actionBarStyle</item>
        <item name="panelBackground">@color/panelBackground</item>
    </style>

</resources>
